version: '3.9'
services:
  backend:
    build:
      context: .
      dockerfile: ./dockerfiles/backend.dockerfile
    ports:
      - '3000:3000'
    volumes:
      - ./backend:/app
      - /app/node_modules
    environment:
      DATABASE_URL: postgres://postgres:postgres@postgres:5432/timely
      SEND_GRID_API_KEY: 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
      FROM_EMAIL: pbrowne@gmail.com

    depends_on:
      - postgres
  postgres:
    build:
      context: .
      dockerfile: ./dockerfiles/postgres.dockerfile
    ports:
      - 5432:5432
    user: postgres
    restart: always
    environment:
      - 'POSTGRES_PASSWORD=postgres'
    # volumes:
    #   - database-data:/var/lib/postgresql/data/
# volumes:
#   # database-data:
