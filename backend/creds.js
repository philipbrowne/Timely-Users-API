const FROM_EMAIL = 'timely.email.sender@gmail.com';
const FROM_PW = 'TimelyPW1234';

// These credentials are only being made available because this is a private respository for a programming assessment.

module.exports = {
  FROM_EMAIL,
  FROM_PW,
};
